diff --git a/node_modules/@deepseek-ai/dsh-mcp-client/lib/index.js b/node_modules/@deepseek-ai/dsh-mcp-client/lib/index.js
index b2f16b4..d2e6ffc 100644
--- a/node_modules/@deepseek-ai/dsh-mcp-client/lib/index.js
+++ b/node_modules/@deepseek-ai/dsh-mcp-client/lib/index.js
@@ -639,7 +639,7 @@ function startConnection(ctx, config, policy) {
 			}
 		});
 		try {
-			await generation.connect(createTransport(config));
+			await generation.connect(createTransport(config), config.protocolVersion !== void 0 ? { protocolVersion: config.protocolVersion } : void 0);
 			if (hasClosed()) {
 				attemptSettled = true;
 				generationDown(generation);
@@ -749,6 +749,7 @@ const Config = z.union([z.object({
 	cwd: z.string().default(""),
 	toolCallTimeoutMs: z.number().default(DEFAULT_TOOL_CALL_TIMEOUT_MS),
 	failOnStartupError: z.boolean().default(false),
+	protocolVersion: z.string().pattern(/^\d{4}-\d{2}-\d{2}$/),
 	reconnect: Reconnect
 }), z.object({
 	transport: z.const("streamable-http"),
@@ -757,6 +758,7 @@ const Config = z.union([z.object({
 	headers: z.dict(String).default({}),
 	toolCallTimeoutMs: z.number().default(DEFAULT_TOOL_CALL_TIMEOUT_MS),
 	failOnStartupError: z.boolean().default(false),
+	protocolVersion: z.string().pattern(/^\d{4}-\d{2}-\d{2}$/),
 	reconnect: Reconnect
 })]);
 /**
