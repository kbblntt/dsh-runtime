window.__ModuleLoader__.load({
	id: "@deepseek-ai/dsh-client-ui-settings-general",
	factory: (require) => {
		var module = { exports: {} };
		var exports = module.exports;
		Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
		let _deepseek_ai_dsh_client_ui_slots = require("@deepseek-ai/dsh-client-ui-slots");
		let react_jsx_runtime = require("react/jsx-runtime");
		let react = require("react");
		let _deepseek_ai_dsh_client_ui_primitives = require("@deepseek-ai/dsh-client-ui-primitives");
		let _deepseek_ai_dsh_client_store = require("@deepseek-ai/dsh-client-store");
		//#region ../../../node_modules/.pnpm/clsx@2.1.1/node_modules/clsx/dist/clsx.mjs
		function r(e) {
			var t, f, n = "";
			if ("string" == typeof e || "number" == typeof e) n += e;
			else if ("object" == typeof e) if (Array.isArray(e)) {
				var o = e.length;
				for (t = 0; t < o; t++) e[t] && (f = r(e[t])) && (n && (n += " "), n += f);
			} else for (f in e) e[f] && (n && (n += " "), n += f);
			return n;
		}
		function clsx() {
			for (var e, t, f = 0, n = "", o = arguments.length; f < o; f++) (e = arguments[f]) && (t = r(e)) && (n && (n += " "), n += t);
			return n;
		}
		//#endregion
		//#region \0dsh-css:/home/runner/work/deepseek-harness/deepseek-harness/packages/client/ui-settings-general/src/client/SettingsRoot.module.css.mjs
		const css$3 = ".VOzbGW_triggerRow{flex:none;align-items:center;gap:8px;width:calc(100% + 4px);margin:4px -2px;display:flex}.VOzbGW_triggerRow.VOzbGW_railRow{width:36px;margin:8px 0 10px}.VOzbGW_trigger{box-sizing:border-box;cursor:pointer;width:auto;min-width:0;height:42px;color:var(--dsw-alias-label-primary);background:0 0;border:none;border-radius:12px;flex:1;align-items:center;gap:8px;margin:0;padding:0 10px 0 8px;font-family:inherit;font-size:14px;line-height:22px;display:flex;overflow:hidden}.VOzbGW_trigger:hover{background:var(--dsw-alias-interactive-bg-hover)}.VOzbGW_trigger.VOzbGW_rail{corner-shape:round;border-radius:50%;flex:none;justify-content:center;gap:0;width:36px;height:36px;margin:0;padding:0}.VOzbGW_triggerLabel{white-space:nowrap;overflow:hidden}.VOzbGW_overlay{z-index:1000;justify-content:center;align-items:center;display:flex;position:fixed;inset:0}.VOzbGW_mask{background:var(--dsw-alias-bg-mask-1);backdrop-filter:var(--dsw-mask-blur);position:absolute;inset:0}.VOzbGW_panel{z-index:1;background:var(--dsw-alias-bg-layer-2);width:min(1120px,calc(100vw - 48px));max-width:calc(100vw - 48px);height:min(800px,100vh - 48px);box-shadow:var(--dsw-elevation-prominent);--dsh-scrollbar-thumb:var(--dsw-alias-scrollbar-bg-l2);--dsh-scrollbar-thumb-hover:var(--dsw-alias-scrollbar-hover-l2);border-radius:32px;display:flex;position:relative;overflow:hidden}.VOzbGW_nav{box-sizing:border-box;flex-direction:column;flex:none;gap:18px;width:188px;padding:22px 12px 0;display:flex}.VOzbGW_navTitle{color:var(--dsw-alias-label-primary);padding:0 12px;font-size:16px;font-weight:500;line-height:24px}.VOzbGW_navList{flex-direction:column;gap:4px;display:flex}.VOzbGW_navCell{box-sizing:border-box;cursor:pointer;height:40px;color:var(--dsw-alias-label-primary);text-align:left;background:0 0;border:none;border-radius:12px;align-items:center;gap:8px;padding:9px 16px 9px 12px;font-family:inherit;font-size:14px;font-weight:400;line-height:22px;display:flex}.VOzbGW_navCell:hover{background:var(--dsw-specific-sidebar-nav-item-hover)}.VOzbGW_navCell.VOzbGW_active{background:var(--dsw-specific-sidebar-nav-item-active)}.VOzbGW_navIcon{flex:none}.VOzbGW_navLabel{white-space:nowrap;text-overflow:ellipsis;flex:1;min-width:0;overflow:hidden}.VOzbGW_content{flex-direction:column;flex:1;min-width:0;display:flex}.VOzbGW_header{box-sizing:border-box;flex:none;justify-content:space-between;align-items:flex-start;gap:8px;height:54px;padding:20px 14px 8px 10px;display:flex}.VOzbGW_actions{justify-content:flex-end;align-items:center;gap:8px;min-width:0;margin-left:auto;display:flex}.VOzbGW_close{cursor:pointer;width:28px;height:28px;color:var(--dsw-alias-label-primary);background:0 0;border:none;border-radius:28px;justify-content:center;align-items:center;padding:0;display:inline-flex}.VOzbGW_close:hover{background:var(--dsw-alias-interactive-bg-hover)}.VOzbGW_options{flex:1;min-height:0;padding:0 24px 24px;overflow-y:auto}.VOzbGW_hiddenLabel{clip:rect(0 0 0 0);white-space:nowrap;width:1px;height:1px;position:absolute;overflow:hidden}";
		const tagId$3 = "@deepseek-ai/dsh-client-ui-settings-general/SettingsRoot.module.css";
		if (typeof document !== "undefined" && document.querySelector("style[data-plugin-css=" + JSON.stringify(tagId$3) + "]") === null) {
			const tag = document.createElement("style");
			tag.dataset.plugin = "@deepseek-ai/dsh-client-ui-settings-general";
			tag.dataset.pluginCss = tagId$3;
			tag.textContent = css$3;
			document.head.appendChild(tag);
		}
		var SettingsRoot_module_css_default = {
			"actions": "VOzbGW_actions",
			"active": "VOzbGW_active",
			"close": "VOzbGW_close",
			"content": "VOzbGW_content",
			"header": "VOzbGW_header",
			"hiddenLabel": "VOzbGW_hiddenLabel",
			"mask": "VOzbGW_mask",
			"nav": "VOzbGW_nav",
			"navCell": "VOzbGW_navCell",
			"navIcon": "VOzbGW_navIcon",
			"navLabel": "VOzbGW_navLabel",
			"navList": "VOzbGW_navList",
			"navTitle": "VOzbGW_navTitle",
			"options": "VOzbGW_options",
			"overlay": "VOzbGW_overlay",
			"panel": "VOzbGW_panel",
			"rail": "VOzbGW_rail",
			"railRow": "VOzbGW_railRow",
			"trigger": "VOzbGW_trigger",
			"triggerLabel": "VOzbGW_triggerLabel",
			"triggerRow": "VOzbGW_triggerRow"
		};
		//#endregion
		//#region lib/types/client/SettingsRoot.js
		/**
		* Settings shell root: the sidebar-foot trigger row plus the centered modal
		* panel (figma 501:29947, 1080x700) with the section nav rail. The shell is
		* a pure composition face — every piece of text (trigger label, panel title,
		* close label, sections) arrives from registrants through slots; accessible
		* names resolve to that content (trigger: its own text; dialog:
		* aria-labelledby the title node; close: visually-hidden slot text). Modal
		* open state and the active section id are component-local viewing state;
		* the onboarding coordinator mounts exactly one ordered registrant while the
		* sessions-derived empty-Hero fact is active. Visible dialog chrome belongs
		* to the step, so a mounted-but-deciding step paints nothing here.
		*/
		const RECOVERY_CONFIRMATION_MS = 2e3;
		/** Nav glyph by section id; unknown ids fall back to the settings gear. */
		function navIcon(id) {
			if (id === "models") return (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.IconDataOutline16, {
				className: SettingsRoot_module_css_default.navIcon,
				size: 16
			});
			if (id === "agent-presets") return (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.IconAgentPresetOutline16, {
				className: SettingsRoot_module_css_default.navIcon,
				size: 16
			});
			if (id === "plugins") return (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.IconPersonalizationOutline16, {
				className: SettingsRoot_module_css_default.navIcon,
				size: 16
			});
			if (id === "archive") return (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.IconArchiveOutline20, {
				className: SettingsRoot_module_css_default.navIcon,
				size: 16
			});
			if (id === "system-prompt") return (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.IconEditOutline16, {
				className: SettingsRoot_module_css_default.navIcon,
				size: 16
			});
			if (id === "sounds") return (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.IconAlarmClockOutline16, {
				className: SettingsRoot_module_css_default.navIcon,
				size: 16
			});
			return (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.IconSettingsOutline16, {
				className: SettingsRoot_module_css_default.navIcon,
				size: 16
			});
		}
		/**
		* The modal layer: full-viewport mask + centered panel. Close paths: the
		* header button, a mask click, and document-level Escape (mounted only while
		* open, so the listener lifetime is the panel's).
		*/
		function SettingsPanel({ rows, renderSlot, activeId, onSelect, onClose }) {
			const active = rows.find((r) => r.id === activeId)?.id ?? rows[0]?.id;
			const titleId = (0, react.useId)();
			(0, react.useEffect)(() => {
				const onKeyDown = (e) => {
					if (e.key === "Escape") onClose();
				};
				document.addEventListener("keydown", onKeyDown);
				return () => {
					document.removeEventListener("keydown", onKeyDown);
				};
			}, [onClose]);
			const closeButton = (0, react.useRef)(null);
			(0, react.useEffect)(() => {
				closeButton.current?.focus();
			}, []);
			return (0, react_jsx_runtime.jsxs)("div", {
				className: SettingsRoot_module_css_default.overlay,
				role: "presentation",
				children: [(0, react_jsx_runtime.jsx)("div", {
					className: SettingsRoot_module_css_default.mask,
					"aria-hidden": "true",
					onClick: onClose
				}), (0, react_jsx_runtime.jsxs)("div", {
					className: SettingsRoot_module_css_default.panel,
					role: "dialog",
					"aria-modal": "true",
					"aria-labelledby": titleId,
					children: [(0, react_jsx_runtime.jsxs)("nav", {
						className: SettingsRoot_module_css_default.nav,
						children: [(0, react_jsx_runtime.jsx)("div", {
							className: SettingsRoot_module_css_default.navTitle,
							id: titleId,
							children: renderSlot("settings.header", {})
						}), (0, react_jsx_runtime.jsx)("div", {
							className: SettingsRoot_module_css_default.navList,
							children: rows.map((row) => (0, react_jsx_runtime.jsxs)("button", {
								type: "button",
								className: clsx(SettingsRoot_module_css_default.navCell, row.id === active && SettingsRoot_module_css_default.active),
								"aria-current": row.id === active ? "true" : void 0,
								onClick: () => {
									onSelect(row.id);
								},
								children: [navIcon(row.id), (0, react_jsx_runtime.jsx)("span", {
									className: SettingsRoot_module_css_default.navLabel,
									children: row.label
								})]
							}, row.id))
						})]
					}), (0, react_jsx_runtime.jsxs)("div", {
						className: SettingsRoot_module_css_default.content,
						children: [(0, react_jsx_runtime.jsxs)("div", {
							className: SettingsRoot_module_css_default.header,
							children: [(0, react_jsx_runtime.jsx)("div", {
								className: SettingsRoot_module_css_default.actions,
								children: renderSlot("settings.action", {})
							}), (0, react_jsx_runtime.jsxs)("button", {
								ref: closeButton,
								type: "button",
								className: SettingsRoot_module_css_default.close,
								onClick: onClose,
								children: [(0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.IconCloseOutline16, { size: 14 }), (0, react_jsx_runtime.jsx)("span", {
									className: SettingsRoot_module_css_default.hiddenLabel,
									children: renderSlot("settings.close", {})
								})]
							})]
						}), (0, react_jsx_runtime.jsx)("div", {
							className: SettingsRoot_module_css_default.options,
							children: active !== void 0 && renderSlot("settings.section", { close: onClose }, { only: active })
						})]
					})]
				})]
			});
		}
		/**
		* Render the settings trigger and panel.
		* @param props - composed slot props (contract/slots.ts).
		* @returns the settings shell element tree.
		*/
		function SettingsRoot(props) {
			const { wide, reconnect, useConnectionState, useSections, useOnboardingSteps, useSessions, renderSlot, t } = props;
			const [open, setOpen] = (0, react.useState)(false);
			const [activeId, setActiveId] = (0, react.useState)(void 0);
			const [completedOnboarding, setCompletedOnboarding] = (0, react.useState)(() => /* @__PURE__ */ new Set());
			const [showRecovery, setShowRecovery] = (0, react.useState)(false);
			const triggerButton = (0, react.useRef)(null);
			const wasOpen = (0, react.useRef)(open);
			const close = (0, react.useCallback)(() => {
				setOpen(false);
				setActiveId(void 0);
			}, []);
			(0, react.useEffect)(() => {
				if (wasOpen.current && !open) triggerButton.current?.focus();
				wasOpen.current = open;
			}, [open]);
			const openSection = (0, react.useCallback)((id) => {
				setActiveId(id);
				setOpen(true);
			}, []);
			const rows = useSections((s) => s);
			const connectionState = useConnectionState((state) => state);
			const previousConnectionState = (0, react.useRef)(connectionState);
			const onboardingSteps = useOnboardingSteps((s) => s);
			const onboardingActive = useSessions((state) => state.phase === "ready" && (state.current === void 0 || state.byId[state.current]?.blank === true));
			const onboardingStep = onboardingActive ? onboardingSteps.find((step) => !completedOnboarding.has(step.id)) : void 0;
			(0, react.useEffect)(() => {
				if (onboardingActive) return;
				setCompletedOnboarding(/* @__PURE__ */ new Set());
			}, [onboardingActive]);
			(0, react.useLayoutEffect)(() => {
				const previous = previousConnectionState.current;
				previousConnectionState.current = connectionState;
				if (connectionState !== "connected") {
					setShowRecovery(false);
					return;
				}
				if (previous !== "disconnected" && previous !== "connecting") return;
				setShowRecovery(true);
				const timeout = window.setTimeout(() => {
					setShowRecovery(false);
				}, RECOVERY_CONFIRMATION_MS);
				return () => {
					window.clearTimeout(timeout);
				};
			}, [connectionState]);
			const completeOnboardingStep = (0, react.useCallback)((id) => {
				setCompletedOnboarding((previous) => {
					if (previous.has(id)) return previous;
					return new Set([...previous, id]);
				});
			}, []);
			let connectionIndicator;
			if (connectionState === "disconnected") connectionIndicator = "disconnected";
			else if (connectionState === "connecting") connectionIndicator = "connecting";
			else if (showRecovery) connectionIndicator = "recovered";
			return (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [
				(0, react_jsx_runtime.jsxs)("div", {
					className: clsx(SettingsRoot_module_css_default.triggerRow, !wide && SettingsRoot_module_css_default.railRow),
					children: [(0, react_jsx_runtime.jsx)("button", {
						ref: triggerButton,
						type: "button",
						className: clsx(SettingsRoot_module_css_default.trigger, !wide && SettingsRoot_module_css_default.rail),
						"aria-haspopup": "dialog",
						"aria-expanded": open,
						onClick: () => {
							setOpen(true);
						},
						children: renderSlot("settings.trigger", { wide })
					}), (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.ConnectionIndicator, {
						state: wide ? connectionIndicator : void 0,
						disconnectedLabel: t("connection.error"),
						reconnectLabel: t("connection.retry"),
						connectingLabel: t("connection.connecting"),
						recoveredLabel: t("connection.connected"),
						reconnectActionLabel: t("connection.reconnect"),
						restartActionLabel: t("connection.restart"),
						onReconnect: reconnect
					})]
				}),
				open && (0, react_jsx_runtime.jsx)(SettingsPanel, {
					rows,
					renderSlot,
					activeId,
					onSelect: setActiveId,
					onClose: close
				}),
				onboardingStep !== void 0 && renderSlot("settings.onboarding", {
					stepId: onboardingStep.id,
					complete: () => {
						completeOnboardingStep(onboardingStep.id);
					},
					openSection
				}, { only: onboardingStep.id })
			] });
		}
		//#endregion
		//#region \0dsh-css:/home/runner/work/deepseek-harness/deepseek-harness/packages/client/ui-settings-general/src/client/chrome.module.css.mjs
		const css$2 = ".UQsH_q_triggerLabel{white-space:nowrap;overflow:hidden}";
		const tagId$2 = "@deepseek-ai/dsh-client-ui-settings-general/chrome.module.css";
		if (typeof document !== "undefined" && document.querySelector("style[data-plugin-css=" + JSON.stringify(tagId$2) + "]") === null) {
			const tag = document.createElement("style");
			tag.dataset.plugin = "@deepseek-ai/dsh-client-ui-settings-general";
			tag.dataset.pluginCss = tagId$2;
			tag.textContent = css$2;
			document.head.appendChild(tag);
		}
		var chrome_module_css_default = { "triggerLabel": "UQsH_q_triggerLabel" };
		//#endregion
		//#region lib/types/client/chrome.js
		/**
		* Shell chrome content registered into the shell's trigger/header seats: the
		* trigger row icon + label (figma sidebar foot) and the panel title text.
		* The shell renders the surrounding chrome (button, nav heading row) and
		* reads each entry's `label` option for aria text.
		*/
		/**
		* Render the trigger row content (icon; label only in the wide column).
		* @param props - composed slot props.
		* @returns the trigger content fragment.
		*/
		function TriggerContent({ wide, t }) {
			return (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [wide ? (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.IconSettingsOutline16, { size: 16 }) : (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.IconSettingsOutline14, { size: 18 }), wide && (0, react_jsx_runtime.jsx)("span", {
				className: chrome_module_css_default.triggerLabel,
				children: t("trigger")
			})] });
		}
		/**
		* Render the panel title text.
		* @param props - composed slot props.
		* @returns the title text node.
		*/
		function HeaderContent({ t }) {
			return (0, react_jsx_runtime.jsx)(react_jsx_runtime.Fragment, { children: t("title") });
		}
		/**
		* Render the close button's visually-hidden label text.
		* @param props - composed slot props.
		* @returns the label text node.
		*/
		function CloseLabel({ t }) {
			return (0, react_jsx_runtime.jsx)(react_jsx_runtime.Fragment, { children: t("close") });
		}
		//#endregion
		//#region \0dsh-css:/home/runner/work/deepseek-harness/deepseek-harness/packages/client/ui-settings-general/src/client/GeneralSection.module.css.mjs
		const css$1 = "._WvWnq_section{flex-direction:column;width:100%;display:flex}._WvWnq_section>[data-slot=\"settings.general.item\"]>:last-child{border-bottom:none}.USP_row{border-bottom:.5px solid var(--dsw-alias-border-l2);flex-direction:column;gap:8px;width:100%;padding:16px 0;display:flex}.USP_title{color:var(--dsw-alias-label-primary);font-size:14px;font-weight:400;line-height:22px}.USP_desc{color:var(--dsw-alias-label-tertiary);font-size:12px;font-weight:400;line-height:18px}.USP_textarea{box-sizing:border-box;resize:vertical;width:100%;min-height:96px;max-height:320px;color:var(--dsw-alias-label-primary);background:var(--dsw-alias-bg-layer-1);border:.5px solid var(--dsw-alias-border-l2);border-radius:10px;padding:8px 10px;font-family:inherit;font-size:13px;line-height:20px;outline:none}.USP_textarea:focus{border-color:var(--dsw-alias-brand-primary)}.USP_textarea::placeholder{color:var(--dsw-alias-label-tertiary)}.USP_textarea:disabled{opacity:.6;cursor:not-allowed}.USP_builtin{color:var(--dsw-alias-label-tertiary);background:var(--dsw-alias-bg-layer-1);border-radius:8px;padding:6px 10px;font-size:12px;line-height:18px;word-break:break-word}.USP_builtinLabel{color:var(--dsw-alias-label-secondary);margin-right:6px}.USP_footer{align-items:center;justify-content:space-between;gap:12px;width:100%;min-height:24px;display:flex}.USP_status{color:var(--dsw-alias-label-secondary);font-size:12px;line-height:18px}.USP_statusOk{color:var(--dsw-alias-state-success-primary)}.USP_statusError{color:var(--dsw-alias-state-error-primary)}.USP_page{border-bottom:none}";
		const tagId$1 = "@deepseek-ai/dsh-client-ui-settings-general/GeneralSection.module.css";
		if (typeof document !== "undefined" && document.querySelector("style[data-plugin-css=" + JSON.stringify(tagId$1) + "]") === null) {
			const tag = document.createElement("style");
			tag.dataset.plugin = "@deepseek-ai/dsh-client-ui-settings-general";
			tag.dataset.pluginCss = tagId$1;
			tag.textContent = css$1;
			document.head.appendChild(tag);
		}
		var GeneralSection_module_css_default = { "section": "_WvWnq_section" };
		//#endregion
		//#region lib/types/client/GeneralSection.js
		/**
		* Render the General section content column.
		* @param props - composed slot props (contract/slots.ts).
		* @returns the section element tree.
		*/
		function GeneralSection({ renderSlot }) {
			return (0, react_jsx_runtime.jsx)("div", {
				className: GeneralSection_module_css_default.section,
				children: renderSlot("settings.general.item", {})
			});
		}
		//#endregion
		//#region \0dsh-css:/home/runner/work/deepseek-harness/deepseek-harness/packages/client/ui-settings-general/src/client/SettingsDocumentAction.module.css.mjs
		const css = ".me01iq_action{align-items:center;gap:8px;min-width:0;display:flex}.me01iq_error{max-width:180px;color:var(--dsw-alias-state-error-primary);text-overflow:ellipsis;white-space:nowrap;font-size:12px;line-height:18px;overflow:hidden}";
		const tagId = "@deepseek-ai/dsh-client-ui-settings-general/SettingsDocumentAction.module.css";
		if (typeof document !== "undefined" && document.querySelector("style[data-plugin-css=" + JSON.stringify(tagId) + "]") === null) {
			const tag = document.createElement("style");
			tag.dataset.plugin = "@deepseek-ai/dsh-client-ui-settings-general";
			tag.dataset.pluginCss = tagId;
			tag.textContent = css;
			document.head.appendChild(tag);
		}
		var SettingsDocumentAction_module_css_default = {
			"action": "me01iq_action",
			"error": "me01iq_error"
		};
		//#endregion
		//#region lib/types/client/SettingsDocumentAction.js
		/** Optional settings-header action for opening a file-backed Host document. */
		/**
		* Render the open-document action only after Host metadata confirms document availability.
		* @param props - header owner props, localized copy, and injected document state.
		* @returns the action, or null while unavailable or unresolved.
		*/
		function SettingsDocumentAction({ controller, useSnapshot, t }) {
			const state = useSnapshot((snapshot) => snapshot);
			(0, react.useEffect)(() => {
				controller.load();
			}, [controller]);
			if (state.status !== "ready") return null;
			return (0, react_jsx_runtime.jsxs)("div", {
				className: SettingsDocumentAction_module_css_default.action,
				children: [state.error === null ? null : (0, react_jsx_runtime.jsx)("span", {
					className: SettingsDocumentAction_module_css_default.error,
					role: "alert",
					children: t("openDocument.error")
				}), (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.Button, {
					variant: "outline",
					size: "sm",
					disabled: state.opening,
					onClick: () => {
						controller.open();
					},
					children: t("openDocument")
				})]
			});
		}
		//#endregion
		//#region lib/types/client/settings-document-store.js
		/** State owner for the optional local settings-document action. */
		/** Derives local-document availability from the shared mirror and invokes the pathless Host-owned open operation. */
		var SettingsDocumentStore = class {
			ctx;
			describeFace;
			/** uSES-safe state source shared by the registered header action. */
			store = (0, _deepseek_ai_dsh_client_store.createSnapshotStore)({
				status: "idle",
				opening: false,
				error: null
			});
			following;
			/**
			* @param ctx - the plugin's context, whose loopback `remote.settings`
			* namespace opens the provider document.
			* @param describeFace - the shared mirror's describe face (`hasDocument` source).
			*/
			constructor(ctx, describeFace) {
				this.ctx = ctx;
				this.describeFace = describeFace;
			}
			/**
			* Begin following the mirror (idempotent) and reflect whether the current
			* provider owns a local document.
			* @returns settlement once the snapshot reflects the mirror.
			*/
			async load() {
				this.following ??= this.describeFace.subscribe(() => {
					this.derive();
				});
				this.store.update((state) => {
					state.status = "loading";
					state.error = null;
				});
				await this.describeFace.ensure();
				this.derive();
			}
			/**
			* Open the loaded document once; concurrent gestures collapse behind the in-flight action.
			* @returns after the native-open request settles, or immediately when unavailable/already opening.
			*/
			async open() {
				const current = this.store.getSnapshot();
				if (current.status !== "ready" || current.opening) return;
				this.store.update((state) => {
					state.opening = true;
					state.error = null;
				});
				try {
					const result = await this.ctx.remote.settings.openSettingsDocument();
					if (!result.ok) {
						const { message } = result.error;
						this.store.update((state) => {
							state.error = message;
						});
					}
				} finally {
					this.store.update((state) => {
						state.opening = false;
					});
				}
			}
			/** Stop following the mirror. */
			dispose() {
				this.following?.();
				this.following = void 0;
			}
			derive() {
				const mirrored = this.describeFace.getSnapshot();
				if (mirrored.view === void 0) {
					if (mirrored.error !== null) this.store.update((state) => {
						state.status = "unavailable";
						state.error = mirrored.error;
					});
					return;
				}
				const { hasDocument } = mirrored.view;
				this.store.update((state) => {
					state.status = hasDocument ? "ready" : "unavailable";
					state.error = null;
				});
			}
		};
		//#endregion
		//#region lib/types/client/ArchiveSection.js
		/** Styles for the Archive settings section (injected once per page). */
		const archiveSectionCss = ".ARCV_section{width:100%;display:flex;flex-direction:column}.ARCV_count{color:var(--dsw-alias-label-secondary);font-size:12px;line-height:18px;padding:2px 12px 10px}.ARCV_empty{color:var(--dsw-alias-label-secondary);font-size:13px;line-height:20px;padding:24px 12px}.ARCV_error{color:var(--dsw-alias-state-error-primary);font-size:12px;line-height:18px;padding:0 12px 8px}.ARCV_row{align-items:center;gap:12px;width:100%;padding:10px 12px;border-radius:12px;display:flex}.ARCV_row:hover{background:var(--dsw-alias-interactive-bg-hover)}.ARCV_main{min-width:0;flex:1;display:flex;flex-direction:column;gap:2px}.ARCV_title{color:var(--dsw-alias-label-primary);font-size:14px;line-height:22px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.ARCV_meta{color:var(--dsw-alias-label-secondary);font-size:12px;line-height:18px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.ARCV_restore{flex:none}";
		const archiveCssTagId = "@deepseek-ai/dsh-client-ui-settings-general/ArchiveSection.module.css";
		if (typeof document !== "undefined" && document.querySelector("style[data-plugin-css=" + JSON.stringify(archiveCssTagId) + "]") === null) {
			const archiveTag = document.createElement("style");
			archiveTag.dataset.plugin = "@deepseek-ai/dsh-client-ui-settings-general";
			archiveTag.dataset.pluginCss = archiveCssTagId;
			archiveTag.textContent = archiveSectionCss;
			document.head.appendChild(archiveTag);
		}
		/**
		* Archive settings section: lists the registry-global archived sessions with
		* their workspace and last activity, and offers a restore action. Rows read
		* the global standard hooks (useSessions/useWorkspaces); the restore action
		* arrives through the registration inject face and stays hidden while the
		* workspace controller is unavailable.
		* @param props - composed slot props (standard hooks, inject face, locale seat).
		* @returns the section element tree.
		*/
		function ArchiveSection({ useSessions, useWorkspaces, unarchiveSession, deleteSession, refreshSessions, t }) {
			const hasSources = useSessions !== void 0 && useWorkspaces !== void 0;
			const byId = useSessions?.((s) => s.byId);
			const archivedIds = useWorkspaces?.((s) => s.archivedSessionIds);
			const workspaceRows = useWorkspaces?.((s) => s.items);
			const currentSessionId = useSessions?.((s) => s.current);
			const [restoringId, setRestoringId] = (0, react.useState)(null);
			const [error, setError] = (0, react.useState)(null);
			const [confirmingId, setConfirmingId] = (0, react.useState)(null);
			const [deletingId, setDeletingId] = (0, react.useState)(null);
			const onRestore = (0, react.useCallback)((sessionId) => {
				if (unarchiveSession === void 0 || restoringId !== null) return;
				setError(null);
				setRestoringId(sessionId);
				Promise.resolve().then(() => unarchiveSession(sessionId)).then(() => {
					setRestoringId((current) => current === sessionId ? null : current);
				}, (reason) => {
					console.warn("session unarchive rejected:", reason);
					setError(t("archive.restoreFailed") + ": " + (reason instanceof Error ? reason.message : String(reason)));
					setRestoringId((current) => current === sessionId ? null : current);
				});
			}, [unarchiveSession, restoringId, t]);
			const onDelete = (0, react.useCallback)((sessionId) => {
				if (deleteSession === void 0 || deletingId !== null || restoringId !== null) return;
				if (confirmingId !== sessionId) {
					setError(null);
					setConfirmingId(sessionId);
					return;
				}
				setConfirmingId(null);
				setError(null);
				setDeletingId(sessionId);
				Promise.resolve().then(() => deleteSession(sessionId)).then(() => {
					setDeletingId((current) => current === sessionId ? null : current);
					if (refreshSessions !== void 0) Promise.resolve().then(() => refreshSessions()).catch(() => void 0);
				}, (reason) => {
					console.warn("session delete rejected:", reason);
					setError(t("archive.deleteFailed") + ": " + (reason instanceof Error ? reason.message : String(reason)));
					setDeletingId((current) => current === sessionId ? null : current);
				});
			}, [deleteSession, refreshSessions, confirmingId, deletingId, restoringId, t]);
			if (!hasSources) return (0, react_jsx_runtime.jsx)("div", {
				className: "ARCV_section",
				children: (0, react_jsx_runtime.jsx)("div", {
					className: "ARCV_empty",
					children: t("archive.empty")
				})
			});
			const ids = archivedIds ?? [];
			const restoreVisible = unarchiveSession !== void 0;
			const deleteVisible = deleteSession !== void 0;
			return (0, react_jsx_runtime.jsxs)("div", {
				className: "ARCV_section",
				children: [
					(0, react_jsx_runtime.jsx)("div", {
						className: "ARCV_count",
						children: t("archive.count", { n: ids.length })
					}),
					error !== null && (0, react_jsx_runtime.jsx)("div", {
						className: "ARCV_error",
						role: "alert",
						children: error
					}),
					ids.length === 0 ? (0, react_jsx_runtime.jsx)("div", {
						className: "ARCV_empty",
						children: t("archive.empty")
					}) : ids.map((sessionId) => {
						const summary = byId[sessionId];
						const owner = workspaceRows.find((w) => w.sessionIds.includes(sessionId));
						const meta = (owner === void 0 ? t("archive.ungrouped") : owner.title) + (summary?.updatedAt === void 0 ? "" : " · " + new Date(summary.updatedAt).toLocaleString());
						const isCurrent = currentSessionId === sessionId;
						return (0, react_jsx_runtime.jsxs)("div", {
							className: "ARCV_row",
							children: [
								(0, react_jsx_runtime.jsxs)("div", {
									className: "ARCV_main",
									children: [
										(0, react_jsx_runtime.jsx)("div", {
											className: "ARCV_title",
											children: summary?.displayTitle ?? sessionId
										}),
										(0, react_jsx_runtime.jsx)("div", {
											className: "ARCV_meta",
											children: meta
										})
									]
								}),
								restoreVisible && (0, react_jsx_runtime.jsx)("div", {
									className: "ARCV_restore",
									children: (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.Button, {
										variant: "outline",
										size: "sm",
										disabled: restoringId !== null || deletingId !== null,
										onClick: () => {
											setConfirmingId(null);
											onRestore(sessionId);
										},
										children: t("archive.restore")
									})
								}),
								deleteVisible && (0, react_jsx_runtime.jsx)("div", {
									className: "ARCV_restore",
									title: isCurrent ? t("archive.deleteCurrentHint") : void 0,
									children: (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.Button, {
										variant: "outline",
										size: "sm",
										disabled: restoringId !== null || deletingId !== null || isCurrent,
										onClick: () => {
											onDelete(sessionId);
										},
										children: confirmingId === sessionId ? t("archive.deleteConfirm") : t("archive.delete")
									})
								})
							]
						}, sessionId);
					})
				]
			});
		}
		//#endregion
		//#region lib/types/client/SystemPromptRow.js
		/** The Host settings namespace carrying the user-defined system prompt. */
		const SYSTEM_PROMPT_SETTINGS_NAMESPACE = "system-prompt";
		/** Row state mirrored from the namespace scope; the plugin body is the only writer. */
		/**
		* Declares the system-prompt row state and write surface.
		* @returns the store handle.
		*/
		function createSystemPromptRowStore() {
			return (0, _deepseek_ai_dsh_client_store.defineStore)({
				init: () => ({
					status: "loading",
					prompt: "",
					revision: -1
				}),
				actions: { sync: (d, status, prompt, revision) => {
					d.status = status;
					d.prompt = prompt;
					d.revision = revision;
				} }
			});
		}
		/**
		* Render the system-prompt row: title + description + a draft textarea with an
		* explicit save action. Saving writes the Host namespace and reports the
		* settled outcome; the displayed value follows the Host while no draft is open.
		* @param props - composed slot props (t, useStore, inject face).
		* @returns the row element tree.
		*/
		function SystemPromptRow({ t, useStore, savePrompt }) {
			const status = useStore((s) => s.status);
			const savedPrompt = useStore((s) => s.prompt);
			const [draft, setDraft] = (0, react.useState)(null);
			const [busy, setBusy] = (0, react.useState)(false);
			const [error, setError] = (0, react.useState)(false);
			const [saved, setSaved] = (0, react.useState)(false);
			const disabled = status !== "ready";
			const value = draft ?? savedPrompt ?? "";
			const dirty = draft !== null && draft !== (savedPrompt ?? "");
			const onChange = (0, react.useCallback)((event) => {
				setDraft(event.target.value);
				setError(false);
				setSaved(false);
			}, []);
			const onSave = (0, react.useCallback)(() => {
				if (busy || !dirty || disabled) return;
				setBusy(true);
				setError(false);
				Promise.resolve().then(() => savePrompt(draft)).then((outcome) => {
					setBusy(false);
					if (outcome === true) {
						setSaved(true);
						setDraft(null);
					} else setError(true);
				});
			}, [busy, dirty, disabled, draft, savePrompt]);
			const note = error ? t("systemPrompt.error") : saved ? t("systemPrompt.saved") : busy ? t("systemPrompt.saving") : "";
			return (0, react_jsx_runtime.jsxs)("div", {
				className: "USP_row USP_page",
				children: [(0, react_jsx_runtime.jsx)("div", {
					className: "USP_title",
					children: t("systemPrompt.title")
				}), (0, react_jsx_runtime.jsx)("div", {
					className: "USP_desc",
					children: t("systemPrompt.description")
				}), (0, react_jsx_runtime.jsxs)("div", {
					className: "USP_builtin",
					children: [(0, react_jsx_runtime.jsx)("span", {
						className: "USP_builtinLabel",
						children: t("systemPrompt.builtinLabel")
					}), t("systemPrompt.builtin")]
				}), (0, react_jsx_runtime.jsx)("textarea", {
					className: "USP_textarea",
					value,
					disabled: disabled || busy,
					placeholder: t("systemPrompt.placeholder"),
					spellCheck: false,
					onChange
				}), (0, react_jsx_runtime.jsxs)("div", {
					className: "USP_footer",
					children: [(0, react_jsx_runtime.jsx)("span", {
						className: error ? "USP_status USP_statusError" : saved ? "USP_status USP_statusOk" : "USP_status",
						role: error ? "alert" : void 0,
						children: disabled ? t("systemPrompt.unavailable") : note
					}), (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.Button, {
						variant: "outline",
						size: "sm",
						disabled: disabled || busy || !dirty,
						onClick: onSave,
						children: busy ? t("systemPrompt.saving") : t("systemPrompt.save")
					})]
				})]
			});
		}
		//#endregion
		//#region lib/types/client/SoundsRow.js
		/**
		* Notification-sound settings section (提示音): one mode per task event —
		* 默认 built-in chime, 自定义 user-picked audio file, or 静音 — with live
		* preview. Configuration (custom audio included) persists in browser
		* localStorage only. The playback engine hangs off client-root facts wired in
		* apply(): `api-session/status` running→false edges (completion),
		* `api-session/error` (failure), and the Session pending-interaction
		* projection (approval waterfall UI appearance).
		*/
		const SOUNDS_STORAGE_KEY = "dsh.sounds.v1";
		const SOUNDS_EVENTS = ["completion", "error", "approval"];
		const SOUNDS_MODES = ["default", "custom", "off"];
		const SOUNDS_MAX_FILE_BYTES = 500 * 1024;
		const SOUNDS_MAX_DATA_URL_CHARS = 14e5;
		const SOUNDS_MIN_INTERVAL_MS = 700;
		const SOUNDS_ERROR_SUPPRESS_MS = 2500;
		const SOUNDS_TRACK_LIMIT = 500;
		/** Built-in chime voices: oscillator wave, peak gain, and [frequency, offset, duration] notes. */
		const SOUNDS_TONES = {
			completion: { wave: "sine", volume: 0.16, notes: [[880, 0, 0.16], [1174.66, 0.12, 0.24]] },
			error: { wave: "square", volume: 0.07, notes: [[392, 0, 0.2], [277.18, 0.18, 0.32]] },
			approval: { wave: "triangle", volume: 0.14, notes: [[987.77, 0, 0.1], [987.77, 0.14, 0.1], [1567.98, 0.28, 0.26]] }
		};
		/** Styles for the notification-sound row (injected once per page). */
		const soundsRowCss = ".DSHS_events{flex-direction:column;gap:8px;width:100%;display:flex}.DSHS_event{align-items:center;flex-wrap:wrap;gap:8px;width:100%;box-sizing:border-box;background:var(--dsw-alias-bg-layer-1);border:.5px solid var(--dsw-alias-border-l2);border-radius:12px;padding:10px 12px;display:flex}.DSHS_eventLabel{flex:none;min-width:88px;color:var(--dsw-alias-label-primary);font-size:13px;line-height:20px}.DSHS_modes{flex:none;display:inline-flex;background:var(--dsw-alias-bg-layer-2);border:.5px solid var(--dsw-alias-border-l2);border-radius:8px;overflow:hidden}.DSHS_mode{cursor:pointer;border:none;background:0 0;color:var(--dsw-alias-label-secondary);font-family:inherit;font-size:12px;line-height:18px;padding:4px 12px}.DSHS_mode+.DSHS_mode{border-left:.5px solid var(--dsw-alias-border-l2)}.DSHS_mode:hover{color:var(--dsw-alias-label-primary)}.DSHS_mode.DSHS_modeActive,.DSHS_mode.DSHS_modeActive:hover{color:var(--dsw-alias-label-primary-inverted);background:var(--dsw-alias-brand-primary)}.DSHS_fileInput{display:none}.DSHS_fileName{min-width:0;max-width:200px;color:var(--dsw-alias-label-secondary);font-size:12px;line-height:18px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.DSHS_eventError{flex-basis:100%;color:var(--dsw-alias-state-error-primary);font-size:12px;line-height:18px}";
		const soundsRowCssTagId = "@deepseek-ai/dsh-client-ui-settings-general/SoundsRow.module.css";
		if (typeof document !== "undefined" && document.querySelector("style[data-plugin-css=" + JSON.stringify(soundsRowCssTagId) + "]") === null) {
			const soundsTag = document.createElement("style");
			soundsTag.dataset.plugin = "@deepseek-ai/dsh-client-ui-settings-general";
			soundsTag.dataset.pluginCss = soundsRowCssTagId;
			soundsTag.textContent = soundsRowCss;
			document.head.appendChild(soundsTag);
		}
		var SoundsRow_module_css_default = {
			"events": "DSHS_events",
			"event": "DSHS_event",
			"eventLabel": "DSHS_eventLabel",
			"modes": "DSHS_modes",
			"mode": "DSHS_mode",
			"modeActive": "DSHS_modeActive",
			"fileInput": "DSHS_fileInput",
			"fileName": "DSHS_fileName",
			"eventError": "DSHS_eventError"
		};
		// --- browser-local configuration store ---------------------------------
		function soundsDefaultConfig() {
			const events = {};
			for (const kind of SOUNDS_EVENTS) events[kind] = { mode: "default" };
			return { version: 1, events };
		}
		function soundsSanitizeEntry(raw) {
			const entry = raw !== null && typeof raw === "object" ? raw : {};
			const clean = { mode: SOUNDS_MODES.includes(entry.mode) ? entry.mode : "default" };
			if (typeof entry.dataUrl === "string" && entry.dataUrl.startsWith("data:") && entry.dataUrl.length <= SOUNDS_MAX_DATA_URL_CHARS) clean.dataUrl = entry.dataUrl;
			if (typeof entry.name === "string" && entry.name !== "") clean.name = entry.name.slice(0, 200);
			return clean;
		}
		function soundsLoadConfig() {
			const base = soundsDefaultConfig();
			if (typeof window === "undefined") return base;
			try {
				const raw = window.localStorage.getItem(SOUNDS_STORAGE_KEY);
				if (raw === null) return base;
				const parsed = JSON.parse(raw);
				const events = parsed !== null && typeof parsed === "object" && parsed.events !== null && typeof parsed.events === "object" ? parsed.events : {};
				for (const kind of SOUNDS_EVENTS) base.events[kind] = soundsSanitizeEntry(events[kind]);
			} catch (error) {
				console.warn("ui-settings-general: reading sounds config failed:", error);
			}
			return base;
		}
		function soundsSaveConfig(config) {
			if (typeof window === "undefined") return;
			try {
				window.localStorage.setItem(SOUNDS_STORAGE_KEY, JSON.stringify(config));
			} catch (error) {
				console.warn("ui-settings-general: persisting sounds config failed:", error);
			}
		}
		let soundsConfig = soundsLoadConfig();
		function getSoundsConfig() {
			return soundsConfig;
		}
		function setSoundsConfig(next) {
			soundsConfig = next;
			soundsSaveConfig(next);
		}
		// --- playback engine -----------------------------------------------------
		let soundsAudioContext = null;
		function soundsEnsureContext() {
			if (typeof window === "undefined") return void 0;
			try {
				const Ctx = window.AudioContext ?? window.webkitAudioContext;
				if (typeof Ctx !== "function") return void 0;
				soundsAudioContext ??= new Ctx();
				return soundsAudioContext;
			} catch {
				return void 0;
			}
		}
		const soundsLastPlayedAt = { completion: 0, error: 0, approval: 0 };
		function soundsPlayDefault(kind) {
			const voice = SOUNDS_TONES[kind];
			const ctx = soundsEnsureContext();
			if (voice === void 0 || ctx === void 0) return;
			try {
				const resumed = ctx.resume();
				if (resumed !== void 0 && typeof resumed.catch === "function") resumed.catch(() => {});
			} catch {}
			const start = ctx.currentTime + 0.02;
			for (const [frequency, offset, duration] of voice.notes) {
				try {
					const osc = ctx.createOscillator();
					const gain = ctx.createGain();
					osc.type = voice.wave;
					osc.frequency.value = frequency;
					gain.gain.setValueAtTime(1e-4, start + offset);
					gain.gain.exponentialRampToValueAtTime(voice.volume, start + offset + 0.015);
					gain.gain.exponentialRampToValueAtTime(1e-4, start + offset + duration);
					osc.connect(gain);
					gain.connect(ctx.destination);
					osc.start(start + offset);
					osc.stop(start + offset + duration + 0.05);
				} catch {}
			}
		}
		function soundsPlayCustom(dataUrl) {
			if (typeof dataUrl !== "string" || !dataUrl.startsWith("data:")) return;
			try {
				const audio = new Audio(dataUrl);
				audio.play().catch(() => {});
			} catch {}
		}
		/**
		* Play the configured sound for one event kind.
		* @param kind - "completion", "error", or "approval".
		* @param options - `preview` skips the same-kind rate limit.
		*/
		function playSound(kind, options = {}) {
			const entry = soundsConfig.events[kind];
			const mode = entry?.mode ?? "default";
			if (mode === "off") return;
			if (options.preview !== true) {
				const now = Date.now();
				if (now - (soundsLastPlayedAt[kind] ?? 0) < SOUNDS_MIN_INTERVAL_MS) return;
				soundsLastPlayedAt[kind] = now;
			}
			if (mode === "custom") soundsPlayCustom(entry?.dataUrl);
			else soundsPlayDefault(kind);
		}
		// Browsers gate audio behind the first user gesture; pre-unlock lazily.
		if (typeof document !== "undefined" && typeof window !== "undefined") {
			const soundsUnlock = () => {
				const ctx = soundsEnsureContext();
				const resumed = ctx?.resume?.();
				if (resumed !== void 0 && typeof resumed.catch === "function") resumed.catch(() => {});
			};
			document.addEventListener("pointerdown", soundsUnlock, { once: true, capture: true });
			document.addEventListener("keydown", soundsUnlock, { once: true, capture: true });
		}
		// --- Host-event triggers (wired in apply) -------------------------------
		const soundsPrevRunning = new Map();
		const soundsErrorAt = new Map();
		function soundsTrackPrune(map) {
			for (const key of map.keys()) {
				if (map.size <= SOUNDS_TRACK_LIMIT) return;
				map.delete(key);
			}
		}
		/** Completion = a session's running→false edge; subagent children stay silent (their parent's turn end chimes). */
		function soundsObserveStatus(sessionId, running, originOf) {
			const prev = soundsPrevRunning.get(sessionId);
			soundsPrevRunning.set(sessionId, running);
			soundsTrackPrune(soundsPrevRunning);
			if (prev !== true || running !== false) return;
			if (originOf?.(sessionId) === "subagent") return;
			if (Date.now() - (soundsErrorAt.get(sessionId) ?? 0) < SOUNDS_ERROR_SUPPRESS_MS) return;
			playSound("completion");
		}
		/** Failure = a live Agent error on a session this page saw running (cold-activation failures stay silent). */
		function soundsObserveError(sessionId, originOf) {
			if (originOf?.(sessionId) === "subagent") return;
			if (soundsPrevRunning.get(sessionId) !== true) return;
			soundsErrorAt.set(sessionId, Date.now());
			soundsTrackPrune(soundsErrorAt);
			playSound("error");
		}
		/** Approval = a new pending approval interaction appearing for any session. */
		const soundsPrevPending = new Map();
		function soundsObservePending(snapshot) {
			for (const [sessionId, interaction] of snapshot) {
				const kind = typeof interaction?.kind === "string" ? interaction.kind : null;
				if (kind === "approval" && soundsPrevPending.get(sessionId) !== "approval") playSound("approval");
				soundsPrevPending.set(sessionId, kind);
			}
			for (const sessionId of [...soundsPrevPending.keys()]) if (!snapshot.has(sessionId)) soundsPrevPending.delete(sessionId);
		}
		// --- row components -------------------------------------------------------
		/**
		* One event's settings line: label, mode segmented control, preview, and the
		* custom-audio picker (the file reader enforces the size cap).
		*/
		function SoundsEventRow({ eventKey, entry, t, onChange }) {
			const fileInput = (0, react.useRef)(null);
			const [status, setStatus] = (0, react.useState)(null);
			const [reading, setReading] = (0, react.useState)(false);
			const mode = entry.mode;
			const previewDisabled = mode === "off" || mode === "custom" && typeof entry.dataUrl !== "string";
			const onPick = () => {
				fileInput.current?.click();
			};
			const onFile = (event) => {
				const file = event.target.files?.[0] ?? null;
				event.target.value = "";
				if (file === null) return;
				if (file.size > SOUNDS_MAX_FILE_BYTES) {
					setStatus(t("sounds.tooLarge"));
					return;
				}
				setReading(true);
				const reader = new FileReader();
				reader.onload = () => {
					setReading(false);
					const dataUrl = typeof reader.result === "string" ? reader.result : "";
					if (!dataUrl.startsWith("data:")) {
						setStatus(t("sounds.invalid"));
						return;
					}
					setStatus(null);
					onChange(eventKey, { mode: "custom", name: file.name, dataUrl });
				};
				reader.onerror = () => {
					setReading(false);
					setStatus(t("sounds.invalid"));
				};
				reader.readAsDataURL(file);
			};
			const onClear = () => {
				setStatus(null);
				onChange(eventKey, { mode: "custom", name: void 0, dataUrl: void 0 });
			};
			return (0, react_jsx_runtime.jsxs)("div", {
				className: SoundsRow_module_css_default.event,
				children: [
					(0, react_jsx_runtime.jsx)("div", {
						className: SoundsRow_module_css_default.eventLabel,
						children: t(`sounds.event.${eventKey}`)
					}),
					(0, react_jsx_runtime.jsx)("div", {
						className: SoundsRow_module_css_default.modes,
						role: "group",
						"aria-label": t(`sounds.event.${eventKey}`),
						children: SOUNDS_MODES.map((candidate) => (0, react_jsx_runtime.jsx)("button", {
							type: "button",
							className: clsx(SoundsRow_module_css_default.mode, candidate === mode && SoundsRow_module_css_default.modeActive),
							"aria-pressed": candidate === mode,
							onClick: () => {
								setStatus(null);
								onChange(eventKey, { mode: candidate });
							},
							children: t(`sounds.mode.${candidate}`)
						}, candidate))
					}),
					(0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.Button, {
						variant: "outline",
						size: "sm",
						disabled: previewDisabled,
						onClick: () => {
							playSound(eventKey, { preview: true });
						},
						children: t("sounds.preview")
					}),
					mode === "custom" && (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, {
						children: [
							(0, react_jsx_runtime.jsx)("input", {
								ref: fileInput,
								type: "file",
								accept: "audio/*",
								className: SoundsRow_module_css_default.fileInput,
								"aria-label": t("sounds.customAudio"),
								onChange: onFile
							}),
							(0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.Button, {
								variant: "outline",
								size: "sm",
								disabled: reading,
								onClick: onPick,
								children: t("sounds.pick")
							}),
							(0, react_jsx_runtime.jsx)("span", {
								className: SoundsRow_module_css_default.fileName,
								children: typeof entry.name === "string" ? entry.name : t("sounds.noFile")
							}),
							typeof entry.dataUrl === "string" && (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.Button, {
								variant: "outline",
								size: "sm",
								onClick: onClear,
								children: t("sounds.clear")
							})
						]
					}),
					status !== null && (0, react_jsx_runtime.jsx)("div", {
						className: SoundsRow_module_css_default.eventError,
						role: "alert",
						children: status
					})
				]
			});
		}
		/**
		* Render the notification-sound section: one mode per task event, browser-local.
		* @param props - composed slot props (locale seat).
		* @returns the row element tree.
		*/
		function SoundsRow({ t }) {
			const [config, setConfigState] = (0, react.useState)(() => getSoundsConfig());
			const onChange = (0, react.useCallback)((eventKey, patch) => {
				const current = getSoundsConfig();
				const next = { version: 1, events: { ...current.events, [eventKey]: { ...current.events[eventKey], ...patch } } };
				setSoundsConfig(next);
				setConfigState(next);
			}, []);
			return (0, react_jsx_runtime.jsxs)("div", {
				className: "USP_row USP_page",
				children: [
					(0, react_jsx_runtime.jsx)("div", {
						className: "USP_title",
						children: t("sounds.title")
					}),
					(0, react_jsx_runtime.jsx)("div", {
						className: "USP_desc",
						children: t("sounds.description")
					}),
					(0, react_jsx_runtime.jsx)("div", {
						className: SoundsRow_module_css_default.events,
						children: SOUNDS_EVENTS.map((eventKey) => (0, react_jsx_runtime.jsx)(SoundsEventRow, {
							eventKey,
							entry: config.events[eventKey] ?? { mode: "default" },
							t,
							onChange
						}, eventKey))
					})
				]
			});
		}
		//#endregion
		//#region lib/types/client/locales.js
		/** Shell chrome and General-nav dictionaries; feature rows own their copy. */
		/** Simplified Chinese dictionary (the key-set source of truth). */
		const zh = {
			"trigger": "设置",
			"title": "设置",
			"close": "关闭",
			"openDocument": "打开配置文件",
			"openDocument.error": "无法打开配置文件",
			"general.nav": "通用设置",
			"systemPrompt.nav": "系统提示词",
		"systemPrompt.title": "系统提示词",
		"systemPrompt.description": "每次任务开始前注入到系统提示词中的自定义内容，追加在内置提示词之后，对所有会话生效；支持 {{model}}、{{cwd}}、{{provider}} 变量",
		"systemPrompt.builtinLabel": "内置提示词（始终生效）：",
		"systemPrompt.builtin": "You are a coding agent powered by the {{model}} model. Your working directory is {{cwd}}.",
		"systemPrompt.placeholder": "输入自定义系统提示词，留空则不追加",
		"systemPrompt.save": "保存",
		"systemPrompt.saving": "保存中…",
		"systemPrompt.saved": "已保存，下一次请求生效",
		"systemPrompt.error": "保存失败，请重试",
		"systemPrompt.unavailable": "设置服务不可用，无法编辑",
			"archive.nav": "归档",
			"archive.count": "共 {n} 个已归档会话",
			"archive.empty": "暂无已归档的会话。可在左侧会话列表的“…”菜单中选择“归档会话”，已归档会话将在这里展示。",
			"archive.restore": "恢复",
			"archive.restoreFailed": "恢复失败",
			"archive.ungrouped": "未分组",
			"archive.delete": "删除",
			"archive.deleteConfirm": "确认删除",
			"archive.deleteFailed": "删除失败",
			"archive.deleteCurrentHint": "当前会话不能在此删除",
			"connection.error": "连接异常",
			"connection.retry": "立即重连",
			"connection.connecting": "连接中",
			"connection.connected": "连接成功",
			"connection.reconnect": "连接异常，点击立即重连",
			"connection.restart": "连接中，点击立即重连",
			"sounds.nav": "提示音",
			"sounds.title": "提示音",
			"sounds.description": "任务事件的声音提醒；自定义音频仅保存在当前浏览器",
			"sounds.event.completion": "任务完成",
			"sounds.event.error": "任务报错",
			"sounds.event.approval": "任务需要提权",
			"sounds.mode.default": "默认",
			"sounds.mode.custom": "自定义",
			"sounds.mode.off": "静音",
			"sounds.preview": "试听",
			"sounds.pick": "选择音频文件",
			"sounds.customAudio": "自定义音频",
			"sounds.noFile": "未选择音频文件",
			"sounds.clear": "移除",
			"sounds.tooLarge": "音频文件过大，请选择 500 KB 以内的文件",
			"sounds.invalid": "无法读取所选音频文件"
		};
		/** English dictionary, checked complete against the zh key set. */
		const en = {
			"trigger": "Settings",
			"title": "Settings",
			"close": "Close",
			"openDocument": "Open configuration file",
			"openDocument.error": "Could not open configuration file",
			"general.nav": "General",
			"systemPrompt.nav": "System prompt",
			"systemPrompt.title": "System prompt",
			"systemPrompt.description": "Custom text injected into the system prompt before every task, appended after the built-in prompt, across all sessions; {{model}}, {{cwd}} and {{provider}} variables are supported",
			"systemPrompt.builtinLabel": "Built-in prompt (always active):",
			"systemPrompt.builtin": "You are a coding agent powered by the {{model}} model. Your working directory is {{cwd}}.",
			"systemPrompt.placeholder": "Type your custom system prompt; leave empty to append nothing",
			"systemPrompt.save": "Save",
			"systemPrompt.saving": "Saving…",
			"systemPrompt.saved": "Saved; applies from the next request",
			"systemPrompt.error": "Save failed, try again",
			"systemPrompt.unavailable": "Settings service unavailable",
			"archive.nav": "Archive",
			"archive.count": "{n} archived sessions",
			"archive.empty": "No archived sessions. Archive a session from the “…” menu in the sidebar; archived sessions appear here.",
			"archive.restore": "Restore",
			"archive.restoreFailed": "Restore failed",
			"archive.ungrouped": "Ungrouped",
			"archive.delete": "Delete",
			"archive.deleteConfirm": "Confirm delete",
			"archive.deleteFailed": "Delete failed",
			"archive.deleteCurrentHint": "The current session cannot be deleted here",
			"connection.error": "Disconnected",
			"connection.retry": "Reconnect now",
			"connection.connecting": "Connecting",
			"connection.connected": "Connected",
			"connection.reconnect": "Disconnected, reconnect now",
			"connection.restart": "Connecting, restart now",
			"sounds.nav": "Notification sounds",
			"sounds.title": "Notification sounds",
			"sounds.description": "Sound cues for task events; custom audio stays in this browser",
			"sounds.event.completion": "Task completed",
			"sounds.event.error": "Task error",
			"sounds.event.approval": "Task needs approval",
			"sounds.mode.default": "Default",
			"sounds.mode.custom": "Custom",
			"sounds.mode.off": "Mute",
			"sounds.preview": "Preview",
			"sounds.pick": "Choose audio file",
			"sounds.customAudio": "Custom audio",
			"sounds.noFile": "No audio file chosen",
			"sounds.clear": "Remove",
			"sounds.tooLarge": "Audio file is too large; choose one under 500 KB",
			"sounds.invalid": "Could not read the chosen audio file"
		};
		//#endregion
		//#region lib/types/client/index.js
		/** Dictionary namespace owned by this plugin (shell chrome + General copy). */
		const NS = "settings";
		/**
		* Required services (cordis fiber inject). The target slots are declared by
		* ui-settings' apply, whose activation order relative to this one is NOT
		* constrained; registrations depend on their slots through `slots.inject()`.
		*/
		const inject = [
			"slots",
			"locale",
			"connection",
			"remote",
			"remote.settings",
			"settingsScope"
		];
		/**
		* Register the `settings` dictionaries, the chrome content, and the General
		* section, each once its slot declaration is on the ledger.
		* @param ctx - client root context.
		*/
		function apply(ctx) {
			ctx.effect(() => ctx.locale.register(NS, {
				zh,
				en
			}), "ui-settings-general: dictionaries");
			let unarchiveSession = void 0;
			let deleteSession = void 0;
			let refreshSessions = void 0;
			ctx.inject(["workspaces"], (wsCtx) => {
				const workspaces = wsCtx.get("workspaces");
				unarchiveSession = (sessionId) => workspaces.unarchiveSession(sessionId);
				deleteSession = (sessionId) => workspaces.deleteSession(sessionId);
			});
			ctx.inject(["sessions"], (sessionsCtx) => {
				refreshSessions = () => sessionsCtx.get("sessions").refresh();
			});
			const archiveInjected = () => ({ unarchiveSession, deleteSession, refreshSessions });
			const connection = ctx.get("connection");
			const t = ctx.locale.bind(NS);
			const documentController = ctx.remote.$host.isLoopback ? new SettingsDocumentStore(ctx, ctx.settingsScope.describe()) : void 0;
			const documentInjected = documentController === void 0 ? void 0 : () => ({
				controller: documentController,
				hooks: { snapshot: documentController.store }
			});
			ctx.effect(() => () => {
				documentController?.dispose();
			}, "ui-settings-general: document action directory");
			let rowsVersion = -1;
			let rowsRevision = -1;
			let rows = [];
			let onboardingVersion = -1;
			let onboardingSteps = [];
			const shellInjected = () => ({
				reconnect: () => {
					connection.reconnect();
				},
				hooks: {
					connectionState: connection.state,
					sections: {
						getSnapshot: () => {
							const version = ctx.slots.getVersion("settings.section");
							const revision = ctx.locale.getSnapshot().revision;
							if (version !== rowsVersion || revision !== rowsRevision) {
								rowsVersion = version;
								rowsRevision = revision;
								rows = ctx.slots.entries("settings.section").map((e) => ({
									/* v8 ignore next -- list-slot registration requires id (SlotCore rejects an entry without one) */
									id: e.options.id ?? "",
									order: e.options.order ?? 0,
									label: (0, _deepseek_ai_dsh_client_ui_slots.resolveSlotLabel)(e.options.label) ?? ""
								})).sort((a, b) => a.order - b.order);
							}
							return rows;
						},
						subscribe: (listener) => {
							const offLedger = ctx.slots.subscribe("settings.section", listener);
							const offLocale = ctx.locale.subscribe(listener);
							return () => {
								offLedger();
								offLocale();
							};
						}
					},
					onboardingSteps: {
						getSnapshot: () => {
							const version = ctx.slots.getVersion("settings.onboarding");
							if (version !== onboardingVersion) {
								onboardingVersion = version;
								onboardingSteps = ctx.slots.entries("settings.onboarding").map((e) => ({
									/* v8 ignore next -- list-slot registration requires id */
									id: e.options.id ?? "",
									order: e.options.order ?? 0
								})).sort((a, b) => a.order - b.order);
							}
							return onboardingSteps;
						},
						subscribe: (listener) => ctx.slots.subscribe("settings.onboarding", listener)
					}
				}
			});
			ctx.slots.inject("sidebar.settings", () => ctx.slots.register({
				name: "sidebar.settings",
				locale: NS,
				children: {
					"settings.trigger": {
						kind: "single",
						scope: "root"
					},
					"settings.header": {
						kind: "single",
						scope: "root"
					},
					"settings.action": {
						kind: "list",
						scope: "root"
					},
					"settings.close": {
						kind: "single",
						scope: "root"
					},
					"settings.section": {
						kind: "list",
						scope: "root"
					},
					"settings.onboarding": {
						kind: "list",
						scope: "root"
					}
				},
				inject: shellInjected
			}, SettingsRoot));
			ctx.slots.inject("settings.trigger", () => ctx.slots.register({
				name: "settings.trigger",
				locale: NS
			}, TriggerContent));
			ctx.slots.inject("settings.header", () => ctx.slots.register({
				name: "settings.header",
				locale: NS
			}, HeaderContent));
			if (documentInjected !== void 0) ctx.slots.inject("settings.action", () => ctx.slots.register({
				name: "settings.action",
				id: "open-document",
				order: 0,
				locale: NS,
				inject: documentInjected
			}, SettingsDocumentAction));
			ctx.slots.inject("settings.close", () => ctx.slots.register({
				name: "settings.close",
				locale: NS
			}, CloseLabel));
			ctx.slots.inject("settings.section", () => ctx.slots.register({
				name: "settings.section",
				id: "general",
				order: 0,
				label: () => t("general.nav"),
				locale: NS,
				children: { "settings.general.item": {
					kind: "list",
					scope: "root"
				} }
			}, GeneralSection));
			ctx.slots.inject("settings.section", () => ctx.slots.register({
				name: "settings.section",
				id: "archive",
				order: 25,
				label: () => t("archive.nav"),
				locale: NS,
				inject: archiveInjected
			}, ArchiveSection));
			// The user-defined system prompt: a standalone settings section over the
			// Host `system-prompt` settings namespace (registered by the web-app
			// bundle, which injects the text into every system-prompt assembly).
			const systemPromptScope = ctx.settingsScope.bind({ namespace: SYSTEM_PROMPT_SETTINGS_NAMESPACE });
			const systemPromptStore = createSystemPromptRowStore();
			// `defineStore` returns the spec handle; the framework binds it when the
			// slot mounts and hands the live actions to the inject callback, so the
			// sync closure writes through `bound` and no-ops before the first mount.
			let boundSystemPrompt;
			const syncSystemPromptRow = () => {
				const snapshot = systemPromptScope.getSnapshot();
				boundSystemPrompt?.sync(snapshot.status, typeof snapshot.value?.prompt === "string" ? snapshot.value.prompt : "", typeof snapshot.revision === "number" ? snapshot.revision : -1);
			};
			ctx.effect(() => systemPromptScope.subscribe(syncSystemPromptRow), "ui-settings-general: system prompt row sync");
			syncSystemPromptRow();
			ctx.slots.inject("settings.section", () => ctx.slots.register({
				name: "settings.section",
				id: "system-prompt",
				order: 4,
				label: () => t("systemPrompt.nav"),
				store: systemPromptStore,
				locale: NS,
				inject: (actions) => {
					boundSystemPrompt = actions;
					syncSystemPromptRow();
					return {
						savePrompt: (text) => systemPromptScope.set("prompt", text).then(() => {
							const settled = systemPromptScope.getSnapshot();
							return settled.status === "ready" && settled.value?.prompt === text;
						})
					};
				}
			}, SystemPromptRow));
			// The notification-sound section: one mode per task event over browser-local config.
			ctx.slots.inject("settings.section", () => ctx.slots.register({
				name: "settings.section",
				id: "sounds",
				order: 6,
				label: () => t("sounds.nav"),
				locale: NS
			}, SoundsRow));
			// Playback triggers — Host-forwarded session facts read at the client root:
			// turn edges and failures from the session event stream, approvals from the
			// Session pending-interaction projection (no waterfall listener needed, so
			// the approval chain order cannot mute the cue).
			let soundsSessions;
			const soundsOriginOf = (sessionId) => {
				try {
					return soundsSessions?.list.getSnapshot().byId[sessionId]?.origin;
				} catch {
					return void 0;
				}
			};
			ctx.inject(["sessions"], (sessionsCtx) => {
				soundsSessions = sessionsCtx.get("sessions");
			});
			ctx.remote.$on("api-session/status", (sessionId, running) => {
				soundsObserveStatus(sessionId, running, soundsOriginOf);
			});
			ctx.remote.$on("api-session/error", (sessionId) => {
				soundsObserveError(sessionId, soundsOriginOf);
			});
			let soundsPendingDisposer = void 0;
			ctx.effect(() => () => {
				soundsPendingDisposer?.();
			}, "ui-settings-general: sounds pending-interaction subscription");
			ctx.inject(["uiSession"], (uiCtx) => {
				if (soundsPendingDisposer !== void 0) return;
				const uiSession = uiCtx.get("uiSession");
				soundsPendingDisposer = uiSession.pendingInteractions.subscribe(() => {
					soundsObservePending(uiSession.pendingInteractions.getSnapshot());
				});
			});
		}
		//#endregion
		exports.SettingsDocumentStore = SettingsDocumentStore;
		exports.apply = apply;
		exports.inject = inject;
		return module.exports;
	}
});

//# sourceMappingURL=client.js.map